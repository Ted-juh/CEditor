# Device Profile Engine MVP Plan

This document defines the first implementation plan for CEditor's device profile and MIDI layer.

The important architectural decision is:

> CEditor should not build a SysEx generator. It should build a device intent compiler.

A panel component should emit a semantic intent:

```text
Set mainSynth.filter.cutoff to 64
```

The device layer decides whether that intent becomes SysEx, CC, NRPN, RPN, Program Change, a multi-message transaction, a delayed sequence, or a no-op because the parameter is read-only.

## Architectural Principle

```text
A panel component never owns synth implementation logic.
A device profile never owns visual layout.
A binding connects a component port to a semantic device parameter.
The backend compiles semantic device intents into MIDI transactions.
```

This keeps the panel builder, device definitions, and live MIDI runtime separate.

## Internal Names

Use broad internal names. Avoid making "SysEx" the center of the architecture.

```text
Panel Builder
  Visual editor, controls, layout, styles, pages, component ports.

Device Profile Engine
  Device definitions, parameters, value model, encoders, checksums, validation, transaction compilation.

Device Runtime
  Device roles, state cache, MIDI destinations, MIDI queue, feedback suppression, monitor events.

Device Profile Designer
  Authoring UI for .ceditor-device files. This comes after the backend spine is stable.
```

## MVP Goal

The first useful version is complete when this flow works:

```text
1. Load a device profile.
2. Create or select a device role called mainSynth.
3. Select a MIDI destination.
4. Open the parameter browser.
5. Drag Filter Cutoff onto a knob/slider.
6. The control adopts label, range, default value, and display metadata.
7. Moving the control updates UI state immediately.
8. The frontend sends a semantic setParameter intent to the backend.
9. The backend validates the value.
10. The backend compiles a MIDI transaction.
11. MIDI preview shows the generated bytes.
12. The runtime queue sends safely, or dry-run mode logs without sending.
13. MIDI monitor logs the result.
14. A second control bound to the same parameter updates from backend state.
15. A test vector for that parameter passes.
```

Anything outside that flow is deferred unless it directly protects the spine.

## Current Implementation Status

Implemented:

- Declarative `.ceditor-device` JSON profiles for internal CC, SysEx, and NRPN test devices.
- Backend Device Profile Engine with semantic parameter compilation, value encoding, checksum support, validation messages, and test vectors.
- Device requests, startup sync declarations, dump parse tests, and a preset browser declaration for the internal SysEx test profile.
- Profile identity declarations, Universal SysEx identity request compilation, identity reply matching, mismatch detection, and session-state surfacing.
- Explicit sync direction policy for pull, push, and live roles. Pull runs the startup request chain; push compiles known runtime/panel values through the paced MIDI transaction path; live preserves the bidirectional echo-safe runtime model.
- First-class MIDI transactions with message bytes, policy metadata, and monitor-friendly fields.
- Device Runtime service with role-to-profile mapping, runtime state cache, dry-run preview, JUCE MIDI input/output discovery, opt-in hardware sending, rate-limit coalescing, and per-message delayed queue resume.
- Standalone transport capability reporting, including MIDI input/output selection, SysEx support, timestamp support, chunked inbound SysEx assembly, and scheduled message support.
- Transport capabilities now expose supported endpoint types plus a VST3/AU/AUv3/CLAP plugin transport scaffold marked as planned/host-dependent, so frontend/runtime code can degrade explicitly before those transports are implemented.
- Async request/response plumbing for startup sync and preset scans: pending requests, timeouts, dump matching/parsing, runtime-state updates, and progress events.
- Outbound bulk dump send jobs with profile-timing-aware chunk previews, paced runtime progress, bridge/store APIs, and cancellation of unsent chunks.
- Bulk/librarian jobs can declare an expected verification collection and surface partial/verified multi-message dump summaries on the job.
- Bulk send jobs include first-pass ACK/NAK policy metadata with retry counters, ACK state, NAK retry reset, and terminal rejection status.
- The Device tab surfaces bulk send progress, ACK/NAK state, verification status, retry counts, dump collection progress, missing/duplicate ranges, and live conflict summaries.
- Device requests can declare a `response.continueRequest`, and partial multi-message collections trigger the declared continue request once while keeping the original pending request open.
- Dump completion metadata for expected message/byte counts, partial failure diagnostics, checksum-failure classification, unsupported-codec classification, and declarative fixed ASCII / nibbled ASCII name codecs.
- A first dump collection model that groups parsed messages by collection id, merges semantic values, tracks received/missing/duplicate address ranges, and reports partial versus complete transfer state.
- Runtime integration for incoming multi-message dump collections, including collection progress events, runtime-state merging, and delayed pending-request resolution until the declared collection is complete.
- Preset-scan entries can surface partial and complete multi-message dump collection summaries while a request is still pending.
- Echo/feedback-loop protection in the web runtime through outbound origin tracking, short echo suppression windows, device-origin state fan-out, and active live-conflict detection when device feedback disagrees with a recent panel edit.
- Bridge APIs for profile listing, parameter listing, destination/input listing, transport capabilities, session state, preview, set-parameter, runtime state, monitor events, diagnostics, raw MIDI preview/actions, sync requests, preset scans, bulk sends, incoming MIDI ingest, and profile tests.
- Frontend Device tab with profile/destination/input selection, parameter compatibility checks, binding creation, metadata adoption, transaction preview, test summary, monitor, diagnostics, pull-sync, and preset-scan controls.
- Local Device Profile Designer validation, save, dry-run preview, and test execution for draft profiles when the native bridge is unavailable.
- External profile import through the native file chooser can load `.ceditor-device`, `.ceditor-device.json`, and `.json` files outside the bundled internal profile folder.
- Identity mismatch override is available from the Device tab and native bridge, marking the session ready with `identityStatus: "overridden"` after the user explicitly accepts a legitimate profile/device mismatch.
- Portable project/panel device-session serialization for role/profile/input/output mappings, with diagnostics for missing profiles and missing ports on restore.
- Device Bindings section on components, including default-safe dry-run behavior.
- Interaction preview emits semantic set-parameter intents for bound control ports.

Still incomplete:

- Plugin transports for VST3, AU, AUv3, and CLAP. The current implementation is standalone/preview oriented and must degrade per host capability, especially for SysEx.
- Device-specific retry/continue policies beyond the first ACK/NAK and single-step continue scaffolds.
- Full librarian workflows for devices that expose dedicated name-list dumps, slot-by-slot patch requests, write-confirmation messages, and device-specific retry/continue handshakes.
- Hardware-in-loop tests against at least one real synth protocol. Real captures are not available in the current work session, so this remains deferred.

## Device Sync Engine Plan

The next architectural layer is the Device Sync Engine. It sits between panel/script intents and the MIDI transport. Its job is to turn "a port exists" into "this panel and this synth agree about state."

```text
Panel / Scripts
    |
    v
Device Sync Engine
    |-- transport capabilities
    |-- lifecycle state machine
    |-- request/response correlation
    |-- send queue and pacing
    |-- state mirror and origin tracking
    |-- dump/preset scan jobs
    v
MidiTransport
```

### 1. Transport Capability Layer

Scripts and profiles should call one runtime surface. They should not know whether MIDI is standalone hardware, host-routed VST3 events, AU callbacks, CLAP events, preview logs, or a future virtual transport.

Minimum capability descriptor:

```json
{
  "transport": "standalone",
  "canSendMidi": true,
  "canReceiveMidi": true,
  "canSendSysex": true,
  "canReceiveSysex": true,
  "ownsPorts": true,
  "supportsTimestamps": true,
  "supportsInputSelection": true,
  "supportsOutputSelection": true,
  "supportsChunkedSysex": true,
  "supportsScheduledMessages": true,
  "maxSysexBytes": 1048576
}
```

Standalone can own ports. Plugin formats usually cannot. VST3 must be treated as capability-dependent because many hosts do not pass SysEx reliably. AU/AUv3 and CLAP need their own transport implementations and tests.

### 2. Lifecycle State Machine

"Connected" is not enough. A device instance needs a state machine:

```text
disconnected
  -> linked          transport exists, identity unknown
  -> identifying     identity request sent
  -> mismatch        identity reply does not match the profile
  -> syncing         startup requests or preset scans running
  -> ready           state is current enough for normal editing
  -> desynced        incoming data or timeout invalidated the mirror
  -> error           transport or protocol failure
```

Startup behavior belongs to this state machine, not to a random UI button. The profile declares startup sync steps, and the runtime owns correlation, timeout, retry, progress, and final ready/error state.

### 3. Async Request/Response Correlation

MIDI has no reliable request id. The runtime must correlate by profile-declared matchers:

```json
{
  "requests": [
    {
      "id": "requestCurrentPatchDump",
      "template": ["F0", "7D", "$deviceId", "00", "01", "F7"],
      "response": { "dump": "currentPatchDump" },
      "timeoutMs": 2000,
      "retries": 1
    }
  ]
}
```

The runtime parks pending requests, matches incoming SysEx against the expected dump declaration, resolves state when parsing succeeds, and emits timeout events when it fails. Scripts should eventually see this as `ctx.device.request(...)` or `await`-style helpers, but the backend must remain the owner of matching and failure handling.

### 4. Send Queue And Pacing

Every outbound transaction must go through a pacing queue. This includes:

```text
single CC/NRPN edits
multi-message NRPN/RPN sequences
request messages
bulk dump chunks
slot-by-slot preset scans
raw MIDI actions
```

Profile timing should support:

```json
{
  "timing": {
    "interMessageDelayMs": 20,
    "bulkChunkBytes": 256,
    "bulkChunkDelayMs": 50,
    "handshakeTimeoutMs": 1000,
    "dumpTimeoutMs": 5000
  }
}
```

The current queue supports rate limiting, coalescing, and per-message delayed resume. The next step is outbound bulk chunking with cancellation and progress.

### 5. Device State Mirror And Echo Suppression

Two-way sync needs a backend mirror of device state and an origin model:

```text
origin=user      panel edit, may send out
origin=device    inbound MIDI, must not echo back out
origin=preset    loaded from stored preset/project
origin=script    generated by automation
```

The runtime should track last outbound parameter/value pairs and suppress identical inbound echoes for a short window. It should also mark dirty/clean state per parameter so the UI can show conflicts and offer pull/push choices.

### 6. Bulk Dumps, Preset Names, And Librarian Jobs

Bulk receive is:

```text
request -> response stream -> reassembly -> checksum/shape validation -> parse -> state update
```

Bulk send is:

```text
serialize state/preset -> chunk -> pace -> monitor progress -> verify or report timeout
```

Preset names require per-profile codecs. Some devices return a name list, some include names in patch dumps, and some require scanning every slot. The runtime should treat preset scanning as a cancellable background job with per-slot status, not as a blocking script.

### 7. Settings, Scripting, And Runtime Boundary

Keep the ownership lines clear:

```text
Device profile/settings:
  identity bytes, parameters, address maps, dump shapes, timing, capabilities, name field layout, sync defaults.

Scripts:
  behavior and orchestration: when to request, how to react, custom name codecs/checksums when declarative rules are insufficient.

Runtime:
  lifecycle state, transport selection, queueing, request correlation, timeouts, retries, state mirror, echo suppression, progress events.
```

### 8. Build Order From Here

Recommended next phases:

```text
1. Promote session role mappings into project serialization. Implemented through the project-device-session schema and panel document path; the dedicated `.ceditor-project` wrapper remains future work.
2. Add outbound bulk-send chunking and progress/cancel APIs. Implemented for raw/SysEx bulk jobs with profile timing defaults, native bridge events, and web store helpers.
3. Expand dump-shape declarations and name codecs against documented protocol examples or synthetic fixtures until real captures are available.
4. Add hardware-in-loop test fixtures once a real synth/capture setup is available.
5. Add plugin transport capability detection for VST3/AU/CLAP.
6. Expose stable script APIs over the runtime primitives once the backend contracts are proven.
```

## Explicitly Deferred

These features are important, but not MVP:

```text
Full librarian workflows
Community profile library
Profile marketplace
PDF/manual import
Learn-from-MIDI capture assistant
Generic panel remapping
Macros
Firmware-specific profile switching UI
Arbitrary unsafe profile scripting
```

## File Boundaries

Keep panels portable and keep synth implementation reusable.

### .cepanel / .ceditor-panel

Stores:

```text
Visual layout
Pages
Components
Styles
Component value ports
Device parameter bindings
Visual/runtime bindings
Raw MIDI/action bindings
Required profile references
Parameter metadata snapshots
Panel scripts/actions
```

Does not primarily store:

```text
Full device profile implementation
User-specific MIDI output names as hard requirements
Hardware-specific live runtime state
```

### .ceditor-device

Stores:

```text
Device profile metadata
Parameters
Message recipes
Value encoders
Checksums
Timing policies
Send policies
Test vectors
Coverage
Trust/status/source metadata
```

Does not store:

```text
Panel layout
User MIDI port selection
User-specific device instances
```

### .ceditor-project

Stores:

```text
Panel references
Device profile references
Device role mappings
MIDI destination selections
Device instance variables
Project-level timing overrides
Session configuration
```

## Schema Versioning

Every persisted format should carry an explicit schema version from day one.

```json
{
  "schemaVersion": 1,
  "minCEditorVersion": "0.9.0"
}
```

Device profiles should also carry a profile version:

```json
{
  "schemaVersion": 1,
  "profileVersion": "1.0.0",
  "minCEditorVersion": "0.9.0"
}
```

Assume migrations will be needed:

```text
deviceProfile v1 -> v2
panel v1 -> v2
project v1 -> v2
```

## Device Profile Schema v1

The MVP profile is JSON. A zip/package format can come later.

Top-level shape:

```json
{
  "schemaVersion": 1,
  "profileVersion": "1.0.0",
  "minCEditorVersion": "0.9.0",
  "id": "test-sysex-synth",
  "name": "Test SysEx Synth",
  "manufacturer": "CEditor",
  "family": "Internal Test",
  "status": "experimental",
  "trust": "local",
  "variables": {},
  "coverage": {},
  "timing": {},
  "parameters": [],
  "messageRecipes": [],
  "tests": [],
  "sources": []
}
```

## Canonical Parameter Value Model

Every parameter can move through several value forms:

```text
Displayed value
  What the user sees or edits.

Semantic value
  The logical value CEditor stores and validates.

Normalized value
  Usually 0.0-1.0 for generic controls.

Encoded MIDI value
  7-bit, 14-bit, enum number, text bytes, packed bits, etc.

Raw bytes
  Final bytes sent to MIDI.
```

Example:

```text
Displayed value: "+12 semitones"
Semantic value: 12
Normalized value: 0.75
Encoded value: 76
Raw MIDI: B0 4A 4C
```

Parameter example:

```json
{
  "id": "filter.cutoff",
  "name": "Filter Cutoff",
  "group": "Filter",
  "type": "integer",
  "range": { "min": 0, "max": 127 },
  "default": 64,
  "display": {
    "mode": "number",
    "unit": "",
    "shortLabel": "Cutoff"
  },
  "normalization": {
    "mode": "linear"
  },
  "encoding": {
    "type": "u7"
  },
  "access": {
    "canRead": true,
    "canWrite": true,
    "realtimeSafe": true,
    "source": "singleParameter",
    "dumpReadable": false,
    "requiresConfirmation": false
  },
  "sendPolicy": {
    "mode": "continuous",
    "coalesce": true,
    "minIntervalMs": 20,
    "sendFinalOnRelease": true
  },
  "messageRecipe": "cutoffCc",
  "ui": {
    "preferredComponent": "Slider"
  }
}
```

Choice example:

```json
{
  "id": "osc.waveform",
  "name": "Oscillator Waveform",
  "group": "Oscillator",
  "type": "choice",
  "default": "saw",
  "choices": [
    { "id": "saw", "label": "Saw", "value": 0 },
    { "id": "square", "label": "Square", "value": 1 },
    { "id": "triangle", "label": "Triangle", "value": 2 }
  ],
  "display": {
    "mode": "choice",
    "shortLabel": "Wave"
  },
  "normalization": {
    "mode": "choiceIndex"
  },
  "encoding": {
    "type": "enum"
  },
  "access": {
    "canRead": true,
    "canWrite": true,
    "realtimeSafe": true,
    "source": "singleParameter"
  },
  "sendPolicy": {
    "mode": "onCommit",
    "coalesce": true
  },
  "messageRecipe": "waveformCc",
  "ui": {
    "preferredComponent": "Combobox"
  }
}
```

Action example:

```json
{
  "id": "actions.requestCurrentPatch",
  "name": "Request Current Patch",
  "type": "action",
  "access": {
    "canRead": false,
    "canWrite": true,
    "realtimeSafe": false,
    "requiresConfirmation": false
  },
  "sendPolicy": {
    "mode": "explicitAction",
    "coalesce": false
  },
  "messageRecipe": "requestCurrentPatch",
  "ui": {
    "preferredComponent": "Button"
  }
}
```

## Access And Capability Flags

Parameters should declare what is safe and possible.

```json
{
  "access": {
    "canRead": true,
    "canWrite": true,
    "realtimeSafe": true,
    "source": "singleParameter",
    "dumpReadable": true,
    "requiresConfirmation": false
  }
}
```

Examples:

```text
Read-only dump field
  canRead: true
  canWrite: false
  source: dumpOnly

Dangerous action
  canRead: false
  canWrite: true
  realtimeSafe: false
  requiresConfirmation: true

Slow SysEx-only parameter
  canRead: true
  canWrite: true
  realtimeSafe: false
```

The UI should use these flags for warnings, disabled actions, and compatibility messages.

## Send Policy

Profiles can define global timing defaults, but parameters need their own send policies.

Continuous parameter:

```json
{
  "sendPolicy": {
    "mode": "continuous",
    "coalesce": true,
    "minIntervalMs": 20,
    "sendFinalOnRelease": true
  }
}
```

Text or patch name:

```json
{
  "sendPolicy": {
    "mode": "onCommit",
    "coalesce": false
  }
}
```

Dangerous action:

```json
{
  "sendPolicy": {
    "mode": "explicitAction",
    "requiresConfirmation": true
  }
}
```

## Message Recipes

Parameters refer to reusable message recipes.

CC example:

```json
{
  "id": "cutoffCc",
  "kind": "cc",
  "channel": "$channel",
  "controller": 74,
  "value": "$encodedValue"
}
```

SysEx example:

```json
{
  "id": "singleParameterWrite",
  "kind": "sysex",
  "template": [
    "F0",
    "7D",
    "$deviceId",
    "$address",
    "$encodedValue",
    "$checksum",
    "F7"
  ],
  "checksum": {
    "type": "sum-7bit",
    "from": "$address",
    "to": "$encodedValue"
  }
}
```

NRPN example:

```json
{
  "id": "nrpn14",
  "kind": "nrpn",
  "channel": "$channel",
  "parameterMsb": 1,
  "parameterLsb": 32,
  "valueResolution": 14,
  "value": "$encodedValue",
  "nullAfterSend": false
}
```

## MIDI Transaction Model

Compiled output is a transaction, not a byte array.

```json
{
  "transactionId": "tx_123",
  "source": {
    "deviceRole": "mainSynth",
    "parameterId": "filter.cutoff",
    "value": 64
  },
  "messages": [
    {
      "kind": "sysex",
      "bytes": "F0 7D 10 20 40 70 F7",
      "delayAfterMs": 20
    }
  ],
  "policy": {
    "coalesce": true,
    "realtimeSafe": true,
    "dryRun": false
  },
  "preview": {
    "displayedValue": "64",
    "semanticValue": 64,
    "normalizedValue": 0.503937,
    "encodedValue": "40",
    "checksumStatus": "ok"
  }
}
```

NRPN transaction:

```json
{
  "messages": [
    { "kind": "cc", "channel": 1, "controller": 99, "value": 1 },
    { "kind": "cc", "channel": 1, "controller": 98, "value": 32 },
    { "kind": "cc", "channel": 1, "controller": 6, "value": 64 },
    { "kind": "cc", "channel": 1, "controller": 38, "value": 0 }
  ]
}
```

Future dump request transaction:

```json
{
  "messages": [
    {
      "kind": "sysex",
      "bytes": "F0 7D 10 11 00 40 F7",
      "delayAfterMs": 100
    }
  ],
  "expectsResponse": {
    "type": "sysex",
    "timeoutMs": 2000,
    "matcher": "currentPatchDump"
  }
}
```

## Device Runtime State

The backend owns the authoritative live state.

```text
DeviceRuntimeState
  role: mainSynth
  profileId: test-sysex-synth
  values:
    filter.cutoff = 64
    filter.resonance = 32
    osc.waveform = saw
```

Frontend state can be optimistic for responsiveness, but backend state is the truth.

UI flow:

```text
UI changes value
-> frontend sends setParameter intent
-> backend validates
-> backend updates runtime state
-> backend compiles transaction
-> backend queues/sends or dry-runs
-> backend emits parameterChanged and midiSent/midiPreview
-> all bound controls sync
```

Future incoming dump flow:

```text
MIDI input receives SysEx
-> backend matches profile/dump type
-> backend parses values
-> backend updates runtime state
-> frontend receives parameter updates
-> bound controls sync
```

## Feedback Loop Prevention

Reserve a feedback policy now, even if full MIDI input is deferred.

```json
{
  "feedback": {
    "receiveUpdates": true,
    "ignoreOwnEchoes": true,
    "echoWindowMs": 250
  }
}
```

Outgoing transactions should be taggable so obvious echoed messages can be ignored later.

## MIDI Destination Abstraction

Do not couple device runtime only to standalone hardware ports.

```text
hardwareOutput
pluginOutput
virtual
previewOnly
none
```

Project device instance example:

```json
{
  "role": "mainSynth",
  "profileId": "test-sysex-synth",
  "variables": {
    "deviceId": 16,
    "channel": 1
  },
  "midiDestination": {
    "type": "hardwareOutput",
    "id": "UM-One MIDI Out"
  },
  "timingOverrides": {
    "minDelayBetweenMessagesMs": 30
  }
}
```

Standalone mode can list/open hardware devices. Plugin mode can use plugin MIDI output or preview-only routing.

## Binding Types

Do not overload one binding system with every meaning.

### Device Parameter Binding

```json
{
  "kind": "deviceParameter",
  "componentId": "knob_1",
  "port": "value",
  "deviceRole": "mainSynth",
  "parameterId": "filter.cutoff",
  "adoptMetadata": true,
  "feedback": {
    "receiveUpdates": true,
    "ignoreOwnEchoes": true
  }
}
```

### Visual Runtime Binding

This is the current CEditor-style binding.

```json
{
  "kind": "visualRuntime",
  "source": "state.hover",
  "target": "Background.Fill.colour"
}
```

### Raw MIDI / Action Binding

```json
{
  "kind": "rawMidiAction",
  "componentId": "button_1",
  "event": "clicked",
  "message": "B0 7B 00"
}
```

Raw actions are useful for panic buttons, identity requests, local on/off, and one-off utility messages.

## Component Ports And Compatibility

Component metadata should declare ports.

```json
{
  "componentType": "Slider",
  "ports": [
    {
      "id": "value",
      "accepts": ["integer", "float", "bipolar"],
      "defaultBindingMode": "continuous"
    }
  ]
}
```

Initial compatibility table:

```text
Knob.value
  integer, float, bipolar, choice-stepped

Slider.value
  integer, float, bipolar

Toggle.state
  boolean, two-choice enum

Combobox.selectedChoice
  choice, enum

TextInput.text
  text, patchName

Button.trigger
  action, momentary, dumpRequest, rawMidiAction

XYPad.x / XYPad.y
  integer, float, normalized

Envelope.attack / decay / sustain / release
  integer, float, time-like parameter
```

Drag/drop outcomes:

```text
Compatible
Compatible with warning
Not compatible
```

Example warning:

```text
Filter Type is a choice parameter.
A knob can control it as a stepped selector, but a dropdown is recommended.
```

## Metadata Adoption

Binding a parameter may update:

```text
Label
Short label
Range
Step
Default value
Choice rows
Unit/suffix
Formatter
Preferred control hints
```

This should be one undoable operation:

```text
Undo: Bind Filter Cutoff to knob_1
```

Users need adoption modes:

```text
Inherit all metadata
Inherit behavior only, keep label
Inherit behavior only, keep display
Manual component, no device binding
```

## Missing Profile Behavior

Panels should open visually even if required profiles are missing.

Panel requirement example:

```json
{
  "requiredProfiles": [
    {
      "role": "mainSynth",
      "profileId": "roland-jv1080",
      "version": ">=1.0.0 <2.0.0"
    }
  ],
  "parameterSnapshots": {
    "mainSynth.filter.cutoff": {
      "name": "Filter Cutoff",
      "type": "integer",
      "range": { "min": 0, "max": 127 }
    }
  }
}
```

When missing:

```text
Panel opens visually.
Bound controls show unresolved binding warnings.
Metadata snapshots keep labels/ranges readable.
MIDI send is disabled for unresolved bindings.
User can install or select a replacement profile.
```

## Validation Levels

Validation should be backend-authoritative.

```text
Error
  Profile cannot load or can produce invalid MIDI.

Warning
  Profile loads but may behave unexpectedly.

Info
  Suggestion or missing optional metadata.
```

Example errors:

```text
MIDI data byte outside 0-127
Unknown encoder
Unknown message recipe
Parameter references missing recipe
Checksum references invalid range
Invalid schema version
```

Example warnings:

```text
No test vectors
No timing policy
Parameter has no group
Choice labels missing
No firmware compatibility metadata
```

Example info:

```text
Parameter has no preferred UI component
No short label supplied
```

## Trust, Status, Coverage, And Sources

Profile metadata should communicate reliability.

```json
{
  "status": "experimental",
  "trust": "local",
  "coverage": {
    "singleParameterWrite": "complete",
    "editBufferDumpRequest": "notImplemented",
    "editBufferDumpParse": "notImplemented",
    "bankDump": "notImplemented",
    "patchNameEdit": "notSupported",
    "realtimeEditing": "partial"
  },
  "sources": [
    {
      "type": "manual",
      "title": "Internal Test Profile Notes",
      "notes": "Created for CEditor compiler tests."
    }
  ],
  "testedWith": []
}
```

Possible trust/status values:

```text
local
imported
community
verified
official
unsafe-scripted
```

MVP should allow declarative profiles only.

## Public Library Quality Gate

Not MVP, but keep these future requirements in mind:

```text
Valid schema
No invalid MIDI bytes
No arbitrary script
At least one test vector
Manufacturer/model metadata
Coverage declaration
Author/source metadata
Version number
```

Higher trust levels can require:

```text
Multiple test vectors
Hardware confirmation
Dump parse tests
No warnings
Maintainer review
```

## Test Profiles Before Real Synths

Create small internal profiles before supporting real hardware.

```text
Test CC Synth
  filter.cutoff via CC 74
  filter.resonance via CC 71
  osc.waveform as enum CC

Test SysEx Synth
  simple header/address/value/checksum

Test NRPN Synth
  7-bit and 14-bit NRPN values
```

These profiles let the backend compiler, transaction model, test vectors, preview, state cache, and UI bindings be tested without manufacturer quirks.

After those pass, add one real proof synth.

## Backend Compiler Responsibilities

The Device Profile Engine should be able to compile without MIDI hardware.

Input:

```json
{
  "deviceRole": "mainSynth",
  "parameterId": "filter.cutoff",
  "value": 64,
  "mode": "dryRun"
}
```

Responsibilities:

```text
Resolve role to device instance.
Resolve profile and parameter.
Validate access flags.
Validate value against parameter type/range/choices.
Normalize value when needed.
Encode value.
Resolve variables.
Compile message recipe.
Compute checksums.
Build transaction.
Apply send policy.
Run dry-run or queue send.
Update runtime state when valid.
Emit preview, monitor, and parameterChanged events.
```

## Bridge API Draft

Initial frontend-to-backend events:

```ts
importDeviceProfile(file)
validateDeviceProfile(profile)
listDeviceProfiles()
listProfileParameters(profileId)
getParameterDescriptor(profileId, parameterId)
compileParameterMessage({ deviceRole, parameterId, value, dryRun })
setParameter({ deviceRole, parameterId, value, interactionPhase })
listMidiDestinations()
setDeviceRoleMapping({ role, profileId, midiDestination, variables, timingOverrides })
runProfileTests(profileId)
```

Backend-to-frontend events:

```ts
deviceProfileLoaded
profileValidationChanged
parameterChanged
midiPreview
midiQueued
midiSent
midiReceived
midiError
deviceConnectionChanged
profileTestsFinished
```

Implementation note: avoid expanding `ValueTreeBridge` indefinitely. Add service classes behind the bridge, such as:

```text
DeviceProfileService
DeviceRuntimeService
MidiService
MidiMonitorService
```

## MIDI Monitor MVP

The monitor should be part of the spine.

Minimum fields:

```text
Timestamp
Direction: in/out/error/preview
Device role
Destination/source
Message type
Semantic meaning
Hex
Status
```

Examples:

```text
18:42:11.203 OUT mainSynth SysEx
filter.cutoff = 64
F0 7D 10 20 40 70 F7
Sent

18:42:11.240 ERROR mainSynth
filter.cutoff = 200
Value outside range 0-127
Not sent
```

## Project Health And Conflict Detection

Complex projects need diagnostics.

Examples:

```text
Error:
  knob_14 is bound to missing parameter filter.drive.

Warning:
  patch.name sends only on commit, but component is configured for continuous send.

Warning:
  mainSynth and bassSynth use same MIDI output and same device ID.

Info:
  profile test-sysex-synth has no dump parser.
```

## Implementation Order

Do not start with a Device Profile Designer UI.

Recommended order:

```text
0. Write schema examples and MVP contract.
1. Build backend compiler without MIDI sending.
2. Add backend test vectors.
3. Add transaction model.
4. Add MIDI preview API.
5. Add MIDI output abstraction.
6. Add queue, rate limiter, and dry-run mode.
7. Add backend runtime state cache.
8. Add frontend profile store.
9. Add component ports and compatibility metadata.
10. Add device parameter bindings.
11. Add parameter browser.
12. Add MIDI monitor.
13. Add raw MIDI/action bindings.
14. Add Device Profile Designer UI.
15. Add dump support.
16. Add community/library features.
```

## Immediate Next Work

The next implementation step after this plan is:

```text
Move from generic bulk verification to device-specific librarian protocols:

1. Pick one documented protocol fixture as the reference target while real captures are unavailable.
2. Add checksum/name-codec round-trip tests from documented or synthetic dumps, then replace/augment them with captured hardware dumps later.
3. Expand explicit continue/next-block policies beyond the current single-step `response.continueRequest` scaffold.
4. Add librarian job orchestration for name-list dumps, slot-by-slot patch requests, write confirmation, and continue handshakes.
5. Add hardware-in-loop validation notes/results when a capture device is available.
```

This should reuse the current dump parser, completion metadata, name codecs, and preset scan job surfaces, but move beyond single-message happy paths.
